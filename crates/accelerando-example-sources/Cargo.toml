[package]
name = "accelerando-example-sources"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Data source adapters for Accelerando (Bookmap replay CSV, ...)."

[dependencies]
accelerando-core = { workspace = true }
